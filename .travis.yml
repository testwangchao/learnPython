language: python
dist: trusty
sudo: required
python:
  - "2.7.12"
# command to install dependencies
install:
  - sudo -i
  - pip install pydes
  - pip list
script:
  - "export PYTHONPATH=$PYTHONPATH:."
  - "python PythonLearnA.py"